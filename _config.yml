# Site Settings

paginate:            5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate, jekyll-feed]

# feed settings
title:		     "{{ site.data.settings.title }} | {{ site.data.settings.tagline }}"
description:   不积跬步无以至千里，不积小流无以成江海
name:     清新版
# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#   path:	     rss-feed.xml
